server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://ewm-db:5432/ewm
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  application:
    name: ewm-main-service
  jpa:
    hibernate:
      ddl-auto: update
stats-server:
  url: ${SERVICES_STATSSERVICE_URI:http://localhost:9090}