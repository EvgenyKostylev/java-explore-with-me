server:
  port: 8080

spring:
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:h2:file:./db/ewm
    username: root
    password: root
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true